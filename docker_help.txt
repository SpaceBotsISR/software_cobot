# Build the container:
docker build -t scobot_ros2 .

#Running the container:

    # Host PC
    xhost + && docker run -it --network=host --ipc=host -e DISPLAY=$DISPLAY -v $PWD/ros2_ws:/ros2_ws -e DISPLAY=host.docker.internal:0 -v /tmp/.X11-unix:/tmp/.X11-unix:rw scobot_ros2

    # Jetson
    docker run -it --network=host --ipc=host -e DISPLAY=$DISPLAY -v $PWD/ros2_ws:/ros2_ws -e DISPLAY=host.docker.internal:0 scobot_ros2  

